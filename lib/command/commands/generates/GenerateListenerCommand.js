'use strict';

const GenerateCommandBase = use('command/commands/generates/GenerateCommandBase');

/**
 * @Command(
 *   value=['generate:listener', 'gl'],
 *   type='generate'
 * )
 */
module.exports = class GenerateListenerCommand extends GenerateCommandBase {

  generate() {

  }

}
